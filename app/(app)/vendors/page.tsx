import ComingSoon from '@/app/components/ComingSoon';

export default function VendorsPage() {
  return (
    <ComingSoon
      title="Vendors"
      description="Keep every vendor in one place — caterers, photographers, florists, and venues, with contacts, contracts, and payment status."
    />
  );
}
